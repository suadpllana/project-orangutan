__pycache__/
*.py[cod]
.pytest_cache/
report.json
*.egg-info/
.venv/
dist/
tasks/*/bundle/tests/reward.txt
tasks/*/bundle/tests/score.*
tasks/*/bundle/tests/junit.xml

# reward artefacts a local verifier run scatters across candidate directories
reward.txt
reward.json
score.txt
score.json

# a verifier run also writes into a `verifier/` subdirectory of each candidate
tasks/*/bundle/verifier/
tasks/*/bundle/tests/verifier/
